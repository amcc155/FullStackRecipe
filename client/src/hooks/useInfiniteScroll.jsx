import { useEffect, useState } from "react";
const useInfiteScroll = (fetchFunction, containerRef = null, hasMore, isLoading, offset = 100)=>{
    const [items,setItems] = useState([]);
    const[offset,setOffset] = useState(0)
    return(
        
    )
}